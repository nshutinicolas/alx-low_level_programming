/**
 * op_add - function to add two values
 * @a: first argument
 * @b: second argument
 * Return:sum of the numbers
 */
int op_add(int a, int b)
	return (a + b);

/**
 * op_mul - function to multiply two values
 * @a: first argument
 * @b: second argument
 * Return: product of two values
 */
int op_mul(int a, int b)
	return (a * b);

/**
 * op_div - function to divide two values
 * @a: first argument
 * @b: second argument
 * Return: division of two numbers
 */
int op_div(int a, int b)
	return (a / b);

/**
 * op_sub - function to subtract two values
 * @a: first argument
 * @b: second argument
 * Return: difference between two numbers
 */
int op_sub(int a, int b)
	return (a / b);

/**
 * op_mod - function to get the mudlus of a value
 * @a:first argument
 * @b:second argument
 * Return: the remainder
 */
int op_mod(int a, int b)
	return (a % b);
