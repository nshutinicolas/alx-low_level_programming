#include "holberton.h"

/**
 * *_memcpy - function that copies memory of src to dest
 * @dest: string to copy to
 * @src: stringg to copy from
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		dest[i] = src[i];
	return (dest);
}
